<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:background="@color/appThemeColor"
        android:gravity="center"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/regist_text"
            android:textColor="@color/appSubThemeColor" />

        <android.support.design.widget.TabLayout
            android:id="@+id/data_tablayout"
            style=""
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="5"
            app:tabGravity="center"
            app:tabIndicatorColor="@color/appSubThemeColor"
            app:tabMode="fixed"
            app:tabSelectedTextColor="@color/defult_tabhiglh_color"
            app:tabTextAppearance="@style/MyTabLayoutTextAppearance"
            app:tabTextColor="@color/defult_tab_color" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:text="@string/login_text"
            android:textColor="@color/appSubThemeColor" />
    </LinearLayout>

    <android.support.v4.view.ViewPager
        android:id="@+id/vp_find"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</LinearLayout>
